name: Deploy Supabase (Legacy Self-Hosted Disabled)

on:
  workflow_dispatch:

concurrency:
  group: deploy-supabase-droplet
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "Self-hosted Supabase deploy workflow is disabled."
          echo "Production is now Supabase Cloud."
